[package]
name = "cirq-control-grammar"
version.workspace = true
edition = "2024"
description = "Tree-sitter grammar for the ngspice .control scripting language"
keywords = ["tree-sitter", "grammar", "ngspice", "control", "spice"]
categories = ["parser-implementations"]
license = "BSD-3-Clause"
repository = "https://github.com/cramt/thevenin"

[dependencies]
tree-sitter = "0.26"

[build-dependencies]
cc = "1"
