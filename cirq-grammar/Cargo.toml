[package]
name = "cirq-grammar"
version = "0.1.0"
edition = "2024"
description = "Tree-sitter grammar for the Cirq circuit description language"
keywords = ["tree-sitter", "grammar", "cirq", "circuit", "parser"]
categories = ["parser-implementations"]
license = "BSD-3-Clause"
repository = "https://github.com/cramt/thevenin"

[dependencies]
tree-sitter = "0.25"

[build-dependencies]
cc = "1"
