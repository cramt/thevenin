// Hierarchical design: a buffer made of two inverters.

module inverter {
    port in: in
    port out: out
    port vdd: inout
    port vss: inout

    param wp = 2u
    param wn = 1u
    param l = 180n

    M1: pmos(vdd -> out, gate: in, bulk: vdd, model: pch, w: wp, l: l)
    M2: nmos(out -> vss, gate: in, bulk: vss, model: nch, w: wn, l: l)
}

module buffer {
    port a: in
    port z: out
    port vdd: inout
    port vss: inout

    inv1: inverter(in: a, out: mid, vdd: vdd, vss: vss)
    inv2: inverter(in: mid, out: z, vdd: vdd, vss: vss)
}

circuit buffer_test {
    model nch: nmos {
        level = 1
        vto = 0.7
        kp = 110u
    }

    model pch: pmos {
        level = 1
        vto = -0.7
        kp = 55u
    }

    Vdd: vsource(vdd -> gnd, dc: 1.8)
    Vin: vsource(in -> gnd,
        pulse: { v1: 0, v2: 1.8, delay: 0, rise: 100p, fall: 100p, width: 5n, period: 10n }
    )

    buf1: buffer(a: in, z: out, vdd: vdd, vss: gnd)

    analysis tran {
        step: 50p
        stop: 30n
    }
}
