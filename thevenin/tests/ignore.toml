# Tests that are ignored because they don't work yet.
# Format: "relative/path/to/file.cir" = "reason"

# === Unimplemented models ===
"bsim1/test.cir" = "BSIM1 model not implemented (US-052)"
"bsim2/test.cir" = "BSIM2 model not implemented (US-053)"

# === BSIM3SOI-DD ===
"bsim3soidd/inv2.cir" = "DC OP: singular matrix (NR non-convergence in BSIM3SOI-DD inverter)"
"bsim3soidd/RampVg2.cir" = "empty output: @m1[Vbs] device parameter query and V(g)/10 expression not supported in .print"
"bsim3soidd/t3.cir" = "~18% Ids error near threshold (BSIM3SOI-DD excess current bug exposed by vfb fix)"
"bsim3soidd/t4.cir" = "~22% Ids error (BSIM3SOI-DD excess current bug exposed by vfb fix)"
"bsim3soidd/t5.cir" = "~1.1% Ids error at Ve=-4 (vfbb sign error in back-gate coupling, see FIXING_HARNESS_TESTS.md)"

# === BSIM3SOI-FD ===
"bsim3soifd/inv2.cir" = "DC OP: singular matrix (NR non-convergence in BSIM3SOI-FD inverter)"
"bsim3soifd/RampVg2.cir" = "empty output: @m1[Vbs] device parameter query and V(g)/10 expression not supported in .print"
"bsim3soifd/t3.cir" = "~5.6% Ids error near threshold (T9 tox→tsi fix correct but exposes compensating bug in Abulk chain)"
"bsim3soifd/t4.cir" = "~4.1% Ids error near threshold (T9 tox→tsi fix correct but exposes compensating bug; remaining ~1.6mV Vth offset)"
"bsim3soifd/t5.cir" = "~2.8% Ids error at Vg=0.62V (T9 tox→tsi fix correct but exposes compensating bug in Abulk chain)"

# === BSIM3SOI-PD ===
"bsim3soipd/inv2.cir" = "DC OP: singular matrix (NR non-convergence in BSIM3SOI-PD inverter)"
"bsim3soipd/RampVg2.cir" = "empty output: @m1[Vbs] device parameter query and V(g)/10 expression not supported in .print"
"bsim3soipd/t3.cir" = "DC OP: singular matrix (BSIM3SOI-PD NR non-convergence)"
"bsim3soipd/t4.cir" = "~5.6% Ids error (BSIM3SOI-PD model value bug, previously timed out)"
"bsim3soipd/t5.cir" = "DC OP: singular matrix (BSIM3SOI-PD NR non-convergence)"

# === General circuits ===
"general/rc.cir" = "vacuous pass: .print tran uses v(1) which filters to empty (formatter produces no data rows for this circuit)"
"general/diffpair.cir" = "reference output file says 'To be done' -- no reference to compare against"
"general/fourbitadder.cir" = "times out (>30s)"
"general/mosamp.cir" = "~35% error at DC OP: Level 2 MOSFET missing velocity saturation/mobility degradation"
"general/rtlinv.cir" = "~4.1% transient error at t=9ns: transition timing shift (dynamic BJT caps improved from 5.96% at 7ns)"
"general/schmitt.cir" = "~31% error at t=293ns settling: output oscillation during switching (DC OP fixed by BJT OFF flag, full voltage-dependent cap approach causes transient timing shift)"

# === HFET ===
"hfet/inverter.cir" = "wrong DC OP: V(3)=1.96V vs expected -0.275V (bistable DCFL inverter: source stepping also converges to Vdd; depletion-mode load pulls output high at every source ramp step)"

# === Regression: misc ===
"regression/misc/asrc-tc-2.cir" = "parameter expressions not yet supported (r={1k + v(9)}) + requires .control scripting"

# === Regression: temper ===
"regression/temper/temper-3.cir" = "Requires TEMPER keyword + .control"

# === Sensitivity ===
"sensitivity/diffpair.cir" = "q3/q4:is sensitivity requires bit-exact LU reuse from NR solver (dense LU refactoring loses CMRR precision)"

# === Transient ===
"transient/fourbitadder.cir" = "times out (>30s)"

# === Transmission lines ===
"transmission/cpl3_4_line.cir" = "~0.8% V(2) error at t=20.3ns: CPL 4-line (improved from 1.0% by R_m off-diagonal clamping; polint compensating bug remains; no MOSFETs)"
"transmission/cpl_ibm2.cir" = "~6.4% error at t=9.65ns: CPL 2-line coupling error (polint Neville fix correct but exposed compensating setup bug; no MOSFETs)"
"transmission/ltra2_2_line.cir" = "~5.8% V(3) error at t=29.3ns: cascaded 2-line circuit accumulates CMOS inverter switching errors through second stage"
"transmission/txl2_3_line.cir" = "~2.4% V(2) error at t=16.2ns: TXL three-line cascade accumulates CMOS inverter switching errors through second/third stages (improved from 4.7% by h1 convolution accumulation fix)"

# === VBIC ===
"vbic/CEamp.cir" = "VBIC AC: ~0.2% gain error at 1.48GHz (self-heating FP, 0.028dB vs 0.027dB tolerance; improved from 6.8MHz/0.066dB by q1 clamp fix)"
"vbic/diffamp.cir" = "VBIC diffamp: NR non-convergence (13-transistor circuit with self-heating, source stepping also fails)"
"vbic/FG.cir" = "VBIC FG: 0.234% DC error at Vb=0.74V (self-heating FP evaluation order, grows linearly with Vrth)"
"vbic/FO.cir" = "VBIC FO: 0.205% DC error at Vc=2.2V (self-heating FP evaluation order, 0.005% above 0.2% tolerance)"
"vbic/temp.cir" = "VBIC temp: 0.226% DC error at Vb=0.57V (self-heating FP evaluation order, grows linearly with Vrth)"

