# Tests that are ignored because they don't work yet.
# Format: "relative/path/to/file.cir" = "reason"

# === Unimplemented models ===
"bsim1/test.cir" = "BSIM1 model not implemented (US-052)"
"bsim2/test.cir" = "BSIM2 model not implemented (US-053)"

# === BSIM3SOI-DD ===
"bsim3soidd/inv2.cir" = "DC OP: singular matrix (NR non-convergence in BSIM3SOI-DD inverter)"
"bsim3soidd/RampVg2.cir" = "floating body Vbs ~0 vs expected ~92mV: @m1[Vbs] query works, junction ceq signs fixed, but DD body voltage equilibrium still wrong"
"bsim3soidd/t3.cir" = "~17% Ids error at Vd=0.04V (floating body: BJT currents fixed to bare exp+BjtA, recombination uses sqrt(exp); remaining error from Vth/channel accuracy + missing Gmc/Gme cross-coupling)"
"bsim3soidd/t4.cir" = "~30% Ids error at Vg=0.33V (tied body: BJT Ic added to drain current; error is fundamental channel current accuracy in subthreshold, not body voltage—t4 is tied body)"
"bsim3soidd/t5.cir" = "~23% Ids error at Vg=0.38V (floating body: BJT currents fixed to bare exp+BjtA; remaining error from Vth/channel accuracy + missing Gmc/Gme cross-coupling)"

# === BSIM3SOI-FD ===
"bsim3soifd/inv2.cir" = "DC OP: singular matrix (NR non-convergence in BSIM3SOI-FD inverter)"
"bsim3soifd/t3.cir" = "~5.3% Ids error at Vg=1.58V,col1 (kb3/dvbd0/dvbd1 binning fixed; remaining error from body coupling chain)"

# === BSIM3SOI-PD ===
"bsim3soipd/inv2.cir" = "DC OP: singular matrix (NR non-convergence in BSIM3SOI-PD inverter)"
"bsim3soipd/t3.cir" = "~134% Ids error at Vg=0.01V (junction ceq signs fixed: convergence restored, large body voltage offset)"
"bsim3soipd/t5.cir" = "~513% Ids error at Vg=0.23V (junction ceq signs fixed: convergence restored, large body voltage offset)"

# === General circuits ===
"general/diffpair.cir" = "reference output file says 'To be done' -- no reference to compare against"
"general/fourbitadder.cir" = "reference output file says 'To be done' -- no reference to compare against"
"general/rtlinv.cir" = "~4.6% transient error at t=9ns: BJT transition timing shift (qb-normalized diffusion charge correct per ngspice but worsened from 4.1% due to compensating error)"
"general/mosamp.cir" = "~35% error at DC OP: Level 2 MOSFET not implemented (uses Level 1 fallback; missing velocity saturation/mobility degradation/short-channel effects)"
"general/schmitt.cir" = "~31% error at t=293ns settling: output oscillation during switching (DC OP fixed by BJT OFF flag; timing shift in BJT voltage-dependent cap model)"

# === HFET ===
"hfet/inverter.cir" = "wrong DC OP: V(3)=1.96V vs expected -0.275V (bistable DCFL inverter: source stepping also converges to Vdd; depletion-mode load pulls output high at every source ramp step)"

# === Regression: misc ===
"regression/misc/alter-vec.cir" = ".control: @v1[pulse] waveform param query + alter command execution not implemented"
"regression/misc/asrc-tc-1.cir" = ".control: undefined vector v(3) — behavioral source (ASRC) nodes not in OP result"
"regression/misc/asrc-tc-2.cir" = "parameter expressions not yet supported (r={1k + v(9)}) + requires .control scripting"
"regression/misc/log-functions-1.cir" = ".control: undefined vector v(b1) — behavioral source (B-source) nodes not in OP result"
"regression/misc/resume-1.cir" = ".control: stop when + resume commands not implemented (requires transient state preservation)"

# === Regression: model ===
"regression/model/binning-1.cir" = ".control: BSIM4 model binning (nmos_tst.1/.2 bin selection by L/W) not implemented"

# === Regression: parser ===
"regression/parser/bxpressn-1.cir" = ".control: undefined vector v(n1155_t) — B-source internal node naming not implemented"
"regression/parser/xpressn-3.cir" = ".control: undefined vector v(n1112_b) — subcircuit internal node lookup not implemented (ceil/floor/nint now supported)"

# === Regression: pz ===
"regression/pz/ac-resistance.cir" = ".control: undefined variable 'i' — complex imaginary unit not implemented in expression evaluator"

# === Regression: sens ===
"regression/sens/sens-ac-1.cir" = ".control: AC sensitivity s[r1]=1.27 vs expected 0.001764 (simulator accuracy issue)"
"regression/sens/sens-ac-2.cir" = ".control: AC sensitivity values wrong (simulator accuracy issue)"



# === Transient ===

# === Transmission lines ===
"transmission/cpl3_4_line.cir" = "~0.8% V(2) error at t=20.3ns: CPL 4-line (improved from 1.0% by R_m off-diagonal clamping; polint compensating bug remains; no MOSFETs)"
"transmission/cpl_ibm2.cir" = "~6.4% error at t=9.65ns: CPL 2-line coupling error (polint Neville fix correct but exposed compensating setup bug; no MOSFETs)"
"transmission/ltra2_2_line.cir" = "~5.8% V(3) error at t=29.3ns: cascaded 2-line circuit accumulates CMOS inverter switching errors through second stage"
"transmission/txl2_3_line.cir" = "~2.4% V(2) error at t=16.2ns: TXL three-line cascade accumulates CMOS inverter switching errors through second/third stages (improved from 4.7% by h1 convolution accumulation fix)"

# === VBIC ===
"vbic/CEamp.cir" = "VBIC AC: ~22% gain error at 6.9GHz (self-heating error grows with frequency; passes near-band points via slope tolerance)"
"vbic/diffamp.cir" = "VBIC diffamp: NR non-convergence (13-transistor circuit with self-heating, source stepping also fails)"
"vbic/FG.cir" = "VBIC FG: ~0.2% base error (3.3% at Vb=0.89V due to slope tolerance masking low-bias points; same companion FP eval order issue as FO/temp)"
"vbic/FO.cir" = "VBIC FO: ~0.2% base error (0.385% at Vc=3.75V; diff=2.123e-7 vs tol=2.107e-7, exceeds by 0.8%; all formulas verified across 80+ sessions)"
"vbic/temp.cir" = "VBIC temp: ~0.23% base error (2.3% at Vb=0.76V due to slope tolerance masking low-bias points; same companion FP eval order issue as FO/FG)"

